import { render } from '@testing-library/react'
import React from 'react'
import { PageHeader } from './PageHeader'

describe('PageHeader', () => {
  it('should render', () => {
    render(<PageHeader />)
  })
})
