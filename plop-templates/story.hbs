import React from 'react';
import { {{pascalCase name}} } from './{{pascalCase name}}';

export default {
  title: 'Components/{{pascalCase name}}',
  component: {{pascalCase name}},
  decorators: [],
  parameters: {}
};

const Story = (args) => <{{pascalCase name}} {...args} />;

export const Default = Story.bind({});
Default.args = {

};
